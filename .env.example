# Frontend: realtime server URL.
# Local development:
VITE_SECRET_TRACKING_URL=http://localhost:5050

# Optional same-browser preview password if the realtime server is unavailable.
# Do NOT treat this VITE value as secure in production because frontend env values are public.
VITE_SECRET_ADMIN_PREVIEW_PASSWORD=preview-only-password
