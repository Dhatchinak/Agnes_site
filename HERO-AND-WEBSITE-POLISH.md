# Hero and homepage polish

- Department of Computer Science is now the primary visual focus.
- College identity lockup is smaller and intentionally secondary.
- Main title uses stronger hierarchy, contrast, spacing, and responsive sizing.
- SF Shift 1 badge, tagline, and actions are refined without competing with the title.
- Upcoming Events area now uses the academic light background and blue institutional CTA.
- Existing Events, Achievements, Gallery, FAQ, motivational quote, and Camera Roll functionality are preserved.

Main updated files:
- frontend/src/index.css
- frontend/src/pages/Home.jsx

Build note: source files were assembled and checked, but the container build could not complete because the uploaded node_modules did not include Vite/Rolldown's Linux native optional binding. Run `npm install` on your machine before `npm run dev` or `npm run build`.
